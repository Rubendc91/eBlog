<div class="posts list-group">
<ol>
{{#each posts as |post|}}
  <ul>
<article class="post">
    <div class="title">
       <a href="post/{{id}}">{{title}}</a>
    </div>
    <div class="body">
        {{post_text}}
    </div>
    <div class="meta">
        <strong>Posted by {{User.username}} on {{ post.createdAt}}</strong>
    </div>
</article>
  </ul>
{{/each}}
</ol>
</div>